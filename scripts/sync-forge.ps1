#Requires -Version 5.1
<#
  RETIRED: upstream-first sync is frozen.
  sunn/forge is canonical. Use scripts/mirror-forge.ps1 instead.
#>
$ErrorActionPreference = "Stop"
throw "sync-forge is retired; sunn/forge is canonical. Use bun run mirror:forge."
